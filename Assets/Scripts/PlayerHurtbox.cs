using UnityEngine;

//THIS script is going to manage the player's hitbox to determine whether they're dealing damage to the enemies or dying to the enemies.
public class PlayerHurtbox : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
